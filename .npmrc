# Render (and npm) treat NODE_ENV=production as "omit devDependencies".
# This repo's `next build` needs Tailwind, TypeScript, and @types.
# The Dashboard build command may not match render.yaml; this file is
# always cloned, so npm ci will still install devDependencies.
include[]=dev
include[]=prod
include[]=optional
